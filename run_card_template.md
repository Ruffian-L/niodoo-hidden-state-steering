# Run card — <short title>

**Ran by:** <which AI / human>   ·   **Date:** <YYYY-MM-DD>   ·   **Tree:** <repo @ commit>
**Verdict:** <PASS / FAIL / MIXED>   <(was failing <prior attempts>), if applicable>

## 1. What we asked
<the question, one plain sentence>

## 2. What we ran
<the prompts/traps, the arms (off vs on), the seeds>

## 3. How we ran it (copy-paste reproducible)
```
<exact command(s)>
```

## 4. What we expected
<the prediction, written before the result>

## 5. What actually happened
<plain narrative — print the correct answer next to every result; never make the reader infer it>

## 6. The scoreboard — the climb
| Attempt | What we tried | Result |
|---|---|---|
| <earlier> | <what we tried> | <result> |
| <earlier> | <what we tried> | <result> |
| **this run** | <what we tried> | **<result>** |

> Read this as the climb: <one line — e.g. "three tries didn't hold; this one did">.

Frame each rung as *what-we-tried → result*, never as fault — a failure is a rung, not blame. First run? Write "no prior attempts — this is rung one" and skip the table.

## 7. The math, in plain words
<the few numbers that matter, each translated — e.g. "latch = 0.81 (0 = noise, 1 = exact pull) → strong">

**Raw data:** `<path>` (`<size>`) — the numbers above are the lens, not the receipt. Kept, not buried.

## 8. Decision note (provenance)
**Decided by / on:** <which AI proposed the turn · date>
<what we turned, which direction, and why — and what it was expected to move>

## 9. Human verification / sign-off
- [ ] The prediction (§4) was recorded before the run
- [ ] I re-ran the command and saw the result above — _initials / date_
- [ ] The numbers match the raw data — _initials_
- [ ] Notes:
